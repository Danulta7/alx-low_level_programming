#include <stdio.h>

/**
 * main - Entry point
 * Return: 0 no error, non zero if erro
 */
int main(void)
{
	char la;

	for (la = '0'; la <= '9'; la++)
	{
		putchar(la);
	}
	putchar('\n');
	return (0);
}
